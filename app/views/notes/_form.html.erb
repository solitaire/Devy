<div class="post_form">
  <%= form_for [@project,@note] do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= f.label :title %>
    <p><%= f.text_field :title %></p>
    <%= f.label :body %>
    <p><%= f.text_area  :body %></p>
    <%= f.label :tag_list %>
    <p><%= f.text_field  :tag_list %></p>
    <p><%= f.submit %></p>
  </div>
<% end %>
</div>